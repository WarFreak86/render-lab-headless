# Codex Kickoff Prompt

You are implementing the Render-Lab headless Shopify storefront.

Before editing any code:
1. Read `AGENTS.md`.
2. Read every file under `docs/`.
3. Review all files under `references/`.
4. Inspect the existing repository architecture, dependencies, Hydrogen version, routes, GraphQL fragments, styles, tests, and deployment configuration.
5. Do not change production DNS or live Cloudflare settings.

Then complete **Phase 0 only** from `docs/BUILD_PHASES.md`.

Return:
- current architecture summary
- existing dependencies and versions
- risks/blockers
- proposed Phase 1 plan
- files you expect to touch in Phase 1

Do not implement Phase 1 yet unless explicitly instructed after the audit.
